# Cluster feature extraction config for the staged EM domain-shift set.
#
# Edit before running on the cluster:
# - data.data_dir
# - results_root
# - experiment_id (optional)

experiment_id: "2026-03-30_domain_shift_em_dinov2-large_head_center224"
results_root: "/home/cfuste/data/DINO-LoRA/dinov2"
task_type: "feat-analysis/domain-shift/em"
modality: "em"

data:
  data_dir: "/home/cfuste/data/datasets/domain_shift/em/images"
  expected_groups: 3

model:
  backbone:
    name: dinov2
    variant: large
    load_backend: torchhub
    weights: null
    repo_dir: null
    preprocess:
      preset: em
  checkpoint: null

runtime:
  batch_size: 8
  num_workers: 4
  device: "auto"
  center_crop_size: 224
  img_size:
    mode: native

clahe_norm: false
