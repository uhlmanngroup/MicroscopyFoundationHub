experiment_id: "2026-01-21_dinov2-base_WITHlora-none_feats-stack2"
results_root: "/Users/cfuste/Documents/Results/DINO-LoRA"
task_type: "feats"

data:
  # Folder with all your mixed images (lucchi-*.png, droso-stack*.tif, etc.)
  data_dir: "/Users/cfuste/Documents/Data/ElectronMicroscopy/composed-dinopeft-unsupervised/"

model:
  # One of: small, base, large, giant
  backbone:
    name: dinov2
    variant: base
    load_backend: torchhub
    weights: null
    repo_dir: null
    preprocess:
      preset: em
  dino_size: "base"
  checkpoint: "/Users/cfuste/Documents/Results/20251201-cluster-backup/seg/paired-lora/2025-01-01_A2_dinopoc_dinov2-base_lora-r16_seg_pairedgrid24372968_rep5_s2_base/ckpts/best_model.pt"

runtime:
  # On the Mac, keep it small-ish and CPU-friendly
  batch_size: 8
  num_workers: 0
  img_size:
    mode: longest_edge
    target: 1022
    patch_multiple: 14
    rounding: floor

  # "auto" = use cuda if available else cpu. You can also force "cpu" or "cuda".
  device: "auto"

clahe_norm: false
