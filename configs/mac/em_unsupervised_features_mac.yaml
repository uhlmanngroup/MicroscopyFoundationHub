experiment_id: "2025-12-02_A5_lucchi+droso_dinov2-base_lora-none_feats"
results_root: "/Users/cfuste/Documents/Results/DINO-LoRA"
task_type: "feats"

data:
  # Folder with all your mixed images (lucchi-*.png, droso-stack*.tif, etc.)
  data_dir: "/Users/cfuste/Documents/Data/ElectronMicroscopy/composed-dinopeft/test/images"

model:
  # One of: small, base, large, giant
  backbone:
    name: dinov2
    variant: base
    load_backend: torchhub
    weights: null
    repo_dir: null
    preprocess:
      preset: em
  dino_size: "base"

runtime:
  # On the Mac, keep it small-ish and CPU-friendly
  batch_size: 8
  num_workers: 0
  img_size:
    mode: longest_edge
    target: 1022
    patch_multiple: 14
    rounding: floor

  # "auto" = use cuda if available else cpu. You can also force "cpu" or "cuda".
  device: "auto"
