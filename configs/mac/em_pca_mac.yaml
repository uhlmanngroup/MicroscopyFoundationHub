experiment_id: "2026-01-21_dinov2-base_WITHlora-none_feats-stack2"
results_root: "/Users/cfuste/Documents/Results/DINO-LoRA"
task_type: "feats"

data:
  input_path: "/Users/cfuste/Documents/Results/DINO-LoRA/feats/2026-01-21_dinov2-base_WITHlora-none_feats-stack2/features.npz"

pca:
  n_components: 50  # pre-UMAP PCA
  plot_dims: [0, 1] # for PCA plots if desired
  whiten: false
  l2norm: false
  seed: 0

umap:
  enabled: true
  n_neighbors: 15
  min_dist: 0.1
  n_components: 2
  metric: euclidean  # or cosine
  seed: 0
  output_path: ...  # optional override; else derive from input

clahe_norm: false
